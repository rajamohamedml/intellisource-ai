---
VERDICT: COMMENT
SUMMARY: The fix correctly exposes failed/omitted class analyses in JSON, logs, and HTML with focused regression coverage.
CONFIDENCE: HIGH
---
KEY_ISSUES:
- Minor: the existing “Classes Analyzed” tile tooltip still says classes were “extracted and described,” which is no longer true when `classes_with_failed_analysis` is non-zero. Reword to “extracted” or “analyzed (including failures).”

PR_SUMMARY: |
  ## Summary
  Fixes #12. Failed or omitted LLM class analyses are now explicitly surfaced rather than being indistinguishable from placeholder descriptions.

  ## Root Cause
  Failed LLM batches and omitted class responses fell back to "Description unavailable." without recording that the class was not actually described.

  ## Fix
  Added per-class `analysis_status`, a metadata failed-analysis count, summary logging, and report tile/badges. The schema version is bumped to 1.4 with defaulted additive fields.

  ## Test Plan
  - Regression coverage verifies failed batches, omitted classes, terse valid descriptions, and report rendering.
  - Builder reports `ruff check .`, `mypy src`, and `pytest` passing.